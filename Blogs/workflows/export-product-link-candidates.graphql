query ProductsForBlogLinks($query: String!) {
  products(first: 20, query: $query) {
    nodes {
      id
      title
      handle
      status
      productType
      onlineStoreUrl
      descriptionHtml
    }
  }
}
